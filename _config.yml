title: HP AI Printer Driver
description: Installation Guide and Release Notes
theme: jekyll-theme-minimal

markdown: kramdown

include:
  - installation-guide.md
  - release-notes.md

defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: default
